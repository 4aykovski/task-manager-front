import React from 'react';
import Card from "../ui/Card/Card";
import classes from "./TelegramCard.module.css"

const TelegramCard = () => {
    return (
        <Card className={classes.TelegramCard}>

        </Card>
    );
};

export default TelegramCard;